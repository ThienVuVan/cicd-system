kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-blob-staging
  namespace: staging
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-blob-staging
  storageClassName: azureblob-nfs-premium